// Array.map() : loop through an array and performance a transformation in each element

const numbers = [1, 2, 3, 4, 5];
console.log(numbers);

const numbersTimesTwo = numbers.map((number) => number * 2);
console.log(numbersTimesTwo);
